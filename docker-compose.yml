version: '2'

services:
  db:
    image: postgres
    volumes:
      - ./sql:/docker-entrypoint-initdb.d/
    ports:
      - "5432:5432"

  front:
    build: .
    depends_on:
      - db
    ports:
      - "3000:3000"
